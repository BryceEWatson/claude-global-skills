# Copy to `.env` (which is git-ignored) and fill in your key. The gemini-image
# skill reads this from the current working directory or next to its script.
# NEVER commit a real key — `.env` is ignored at the repo root for this reason.
GEMINI_API_KEY=
